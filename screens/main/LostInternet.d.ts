/**
 * Intentional addition — recreation of the Figma FRAME "LostInternet" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface LostInternetProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const LostInternet: React.FC<LostInternetProps>;
export default LostInternet;
