/**
 * Intentional addition — recreation of the Figma FRAME "MainReadme" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface MainReadmeProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const MainReadme: React.FC<MainReadmeProps>;
export default MainReadme;
