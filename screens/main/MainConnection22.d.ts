/**
 * Intentional addition — recreation of the Figma FRAME "MainConnection22" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface MainConnection22Props {
  className?: string;
  style?: React.CSSProperties;
}
export declare const MainConnection22: React.FC<MainConnection22Props>;
export default MainConnection22;
