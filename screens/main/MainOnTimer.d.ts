/**
 * Intentional addition — recreation of the Figma FRAME "MainOnTimer" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface MainOnTimerProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const MainOnTimer: React.FC<MainOnTimerProps>;
export default MainOnTimer;
