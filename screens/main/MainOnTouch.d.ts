/**
 * Intentional addition — recreation of the Figma FRAME "MainOnTouch" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface MainOnTouchProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const MainOnTouch: React.FC<MainOnTouchProps>;
export default MainOnTouch;
