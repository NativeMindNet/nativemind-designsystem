/**
 * Intentional addition — recreation of the Figma FRAME "LostInternetStart" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface LostInternetStartProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const LostInternetStart: React.FC<LostInternetStartProps>;
export default LostInternetStart;
