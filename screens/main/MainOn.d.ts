/**
 * Intentional addition — recreation of the Figma FRAME "MainOn" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface MainOnProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const MainOn: React.FC<MainOnProps>;
export default MainOn;
