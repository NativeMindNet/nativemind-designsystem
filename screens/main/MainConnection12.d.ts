/**
 * Intentional addition — recreation of the Figma FRAME "MainConnection12" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface MainConnection12Props {
  className?: string;
  style?: React.CSSProperties;
}
export declare const MainConnection12: React.FC<MainConnection12Props>;
export default MainConnection12;
