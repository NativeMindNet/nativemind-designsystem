/**
 * Intentional addition — recreation of the Figma FRAME "MainDisconectTimer" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface MainDisconectTimerProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const MainDisconectTimer: React.FC<MainDisconectTimerProps>;
export default MainDisconectTimer;
