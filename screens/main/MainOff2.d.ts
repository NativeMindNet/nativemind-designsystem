/**
 * Intentional addition — recreation of the Figma FRAME "MainOff2" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface MainOff2Props {
  className?: string;
  style?: React.CSSProperties;
}
export declare const MainOff2: React.FC<MainOff2Props>;
export default MainOff2;
