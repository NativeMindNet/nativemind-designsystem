/**
 * Intentional addition — recreation of the Figma FRAME "MainDisconect" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface MainDisconectProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const MainDisconect: React.FC<MainDisconectProps>;
export default MainDisconect;
