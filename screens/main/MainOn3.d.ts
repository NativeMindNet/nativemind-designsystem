/**
 * Intentional addition — recreation of the Figma FRAME "MainOn3" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface MainOn3Props {
  className?: string;
  style?: React.CSSProperties;
}
export declare const MainOn3: React.FC<MainOn3Props>;
export default MainOn3;
