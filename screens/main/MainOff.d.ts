/**
 * Intentional addition — recreation of the Figma FRAME "MainOff" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface MainOffProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const MainOff: React.FC<MainOffProps>;
export default MainOff;
