/**
 * Intentional addition — recreation of the Figma FRAME "AppsSearchOpen" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface AppsSearchOpenProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const AppsSearchOpen: React.FC<AppsSearchOpenProps>;
export default AppsSearchOpen;
