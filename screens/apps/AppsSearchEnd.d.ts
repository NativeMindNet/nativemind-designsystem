/**
 * Intentional addition — recreation of the Figma FRAME "AppsSearchEnd" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface AppsSearchEndProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const AppsSearchEnd: React.FC<AppsSearchEndProps>;
export default AppsSearchEnd;
