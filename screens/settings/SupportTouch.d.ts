/**
 * Intentional addition — recreation of the Figma FRAME "SupportTouch" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface SupportTouchProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const SupportTouch: React.FC<SupportTouchProps>;
export default SupportTouch;
