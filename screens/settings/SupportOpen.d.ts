/**
 * Intentional addition — recreation of the Figma FRAME "SupportOpen" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface SupportOpenProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const SupportOpen: React.FC<SupportOpenProps>;
export default SupportOpen;
