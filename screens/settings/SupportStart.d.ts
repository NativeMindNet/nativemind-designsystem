/**
 * Intentional addition — recreation of the Figma FRAME "SupportStart" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface SupportStartProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const SupportStart: React.FC<SupportStartProps>;
export default SupportStart;
