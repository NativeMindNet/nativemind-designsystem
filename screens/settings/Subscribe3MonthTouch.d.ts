/**
 * Intentional addition — recreation of the Figma FRAME "Subscribe3MonthTouch" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface Subscribe3MonthTouchProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const Subscribe3MonthTouch: React.FC<Subscribe3MonthTouchProps>;
export default Subscribe3MonthTouch;
