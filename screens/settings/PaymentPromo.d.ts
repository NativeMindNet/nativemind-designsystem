/**
 * Intentional addition — recreation of the Figma FRAME "PaymentPromo" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface PaymentPromoProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const PaymentPromo: React.FC<PaymentPromoProps>;
export default PaymentPromo;
