/**
 * Intentional addition — recreation of the Figma FRAME "Settings4" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface Settings4Props {
  className?: string;
  style?: React.CSSProperties;
}
export declare const Settings4: React.FC<Settings4Props>;
export default Settings4;
