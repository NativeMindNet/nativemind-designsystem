/**
 * Intentional addition — recreation of the Figma FRAME "PayStart" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface PayStartProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const PayStart: React.FC<PayStartProps>;
export default PayStart;
