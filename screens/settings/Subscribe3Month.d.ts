/**
 * Intentional addition — recreation of the Figma FRAME "Subscribe3Month" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface Subscribe3MonthProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const Subscribe3Month: React.FC<Subscribe3MonthProps>;
export default Subscribe3Month;
