/**
 * Intentional addition — recreation of the Figma FRAME "Subscribe12MonthTouch" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface Subscribe12MonthTouchProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const Subscribe12MonthTouch: React.FC<Subscribe12MonthTouchProps>;
export default Subscribe12MonthTouch;
