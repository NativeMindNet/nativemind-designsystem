/**
 * Intentional addition — recreation of the Figma FRAME "SettingsMainOff" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface SettingsMainOffProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const SettingsMainOff: React.FC<SettingsMainOffProps>;
export default SettingsMainOff;
