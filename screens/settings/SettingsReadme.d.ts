/**
 * Intentional addition — recreation of the Figma FRAME "SettingsReadme" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface SettingsReadmeProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const SettingsReadme: React.FC<SettingsReadmeProps>;
export default SettingsReadme;
