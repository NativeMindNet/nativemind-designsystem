/**
 * Intentional addition — recreation of the Figma FRAME "PayOpen" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface PayOpenProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const PayOpen: React.FC<PayOpenProps>;
export default PayOpen;
