/**
 * Intentional addition — recreation of the Figma FRAME "SupportAddOpen" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface SupportAddOpenProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const SupportAddOpen: React.FC<SupportAddOpenProps>;
export default SupportAddOpen;
