/**
 * Intentional addition — recreation of the Figma FRAME "Settings2" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface Settings2Props {
  className?: string;
  style?: React.CSSProperties;
}
export declare const Settings2: React.FC<Settings2Props>;
export default Settings2;
