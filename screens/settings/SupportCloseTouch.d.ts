/**
 * Intentional addition — recreation of the Figma FRAME "SupportCloseTouch" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface SupportCloseTouchProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const SupportCloseTouch: React.FC<SupportCloseTouchProps>;
export default SupportCloseTouch;
