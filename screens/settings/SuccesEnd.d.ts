/**
 * Intentional addition — recreation of the Figma FRAME "SuccesEnd" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface SuccesEndProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const SuccesEnd: React.FC<SuccesEndProps>;
export default SuccesEnd;
