/**
 * Intentional addition — recreation of the Figma FRAME "Payment2" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface Payment2Props {
  className?: string;
  style?: React.CSSProperties;
}
export declare const Payment2: React.FC<Payment2Props>;
export default Payment2;
