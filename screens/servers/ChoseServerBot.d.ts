/**
 * Intentional addition — recreation of the Figma FRAME "ChoseServerBot" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface ChoseServerBotProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const ChoseServerBot: React.FC<ChoseServerBotProps>;
export default ChoseServerBot;
