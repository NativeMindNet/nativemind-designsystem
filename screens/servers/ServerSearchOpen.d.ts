/**
 * Intentional addition — recreation of the Figma FRAME "ServerSearchOpen" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface ServerSearchOpenProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const ServerSearchOpen: React.FC<ServerSearchOpenProps>;
export default ServerSearchOpen;
