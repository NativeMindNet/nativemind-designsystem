/**
 * Intentional addition — recreation of the Figma FRAME "ServerSearch" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface ServerSearchProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const ServerSearch: React.FC<ServerSearchProps>;
export default ServerSearch;
