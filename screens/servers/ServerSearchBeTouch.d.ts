/**
 * Intentional addition — recreation of the Figma FRAME "ServerSearchBeTouch" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface ServerSearchBeTouchProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const ServerSearchBeTouch: React.FC<ServerSearchBeTouchProps>;
export default ServerSearchBeTouch;
