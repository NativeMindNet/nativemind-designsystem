/**
 * Intentional addition — recreation of the Figma FRAME "ChoseServer" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface ChoseServerProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const ChoseServer: React.FC<ChoseServerProps>;
export default ChoseServer;
