/**
 * Intentional addition — recreation of the Figma FRAME "ServerSearchLoad" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface ServerSearchLoadProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const ServerSearchLoad: React.FC<ServerSearchLoadProps>;
export default ServerSearchLoad;
