/**
 * Intentional addition — recreation of the Figma FRAME "ServersReadme" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface ServersReadmeProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const ServersReadme: React.FC<ServersReadmeProps>;
export default ServersReadme;
