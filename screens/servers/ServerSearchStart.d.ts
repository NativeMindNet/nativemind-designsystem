/**
 * Intentional addition — recreation of the Figma FRAME "ServerSearchStart" (screen layer),
 * not a component family. Named after its frame; see readme.md > Screen layer.
 */
import * as React from 'react';
export interface ServerSearchStartProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const ServerSearchStart: React.FC<ServerSearchStartProps>;
export default ServerSearchStart;
