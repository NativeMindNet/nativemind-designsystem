import * as React from 'react';
export interface Clock941Props {
  className?: string;
  style?: React.CSSProperties;
  /** Text content; defaults to "9:41". *
 * Kit family `9:41` — the iPhone status-bar clock label. Renamed only because a component name cannot begin with a digit.
 */
  text1?: string;
}
export declare const Clock941: React.FC<Clock941Props>;
export default Clock941;
